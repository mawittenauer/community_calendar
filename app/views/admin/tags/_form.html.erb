<%= form_with(model: admin_tag) do |form| %>
  <% if admin_tag.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_tag.errors.count, "error") %> prohibited this admin_tag from being saved:</h2>

      <ul>
        <% admin_tag.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :slug, style: "display: block" %>
    <%= form.text_field :slug %>
  </div>

  <div>
    <%= form.label :is_active, style: "display: block" %>
    <%= form.checkbox :is_active %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
